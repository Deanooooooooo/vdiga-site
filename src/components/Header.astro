---
import { site, withBase } from "../data/site";
---

<header class="header">
  <div class="container nav">
    <a class="brand" href={withBase("/")} aria-label="Vdiga начало">
      <span class="brand-mark">V</span>
      <span>Vdiga</span>
    </a>
    <nav class="nav-links" aria-label="Основна навигация">
      {site.nav.map((item) => <a href={item.href}>{item.label}</a>)}
    </nav>
    <div class="nav-actions">
      <a class="btn btn-secondary btn-small" href={withBase("/login")}>Вход</a>
      <a class="btn btn-primary btn-small" href={withBase("/signup")}>Регистрация</a>
      <a class="btn btn-secondary btn-small nav-demo" href={withBase("/demo")}>Демо</a>
    </div>
  </div>
</header>
